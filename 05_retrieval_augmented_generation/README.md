# Retrieval-augmented generation

Giving a model access to information beyond its training weights and
context window, retrieved on demand — built up from raw similarity
search rather than a RAG framework.

No investigations published here yet.
